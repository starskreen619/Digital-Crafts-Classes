def add_nums(a, b):
    print(a + b)


add_nums(6, 8)

############################


def total_count(stringlist):
    length_string = len(stringlist)
    total_chars = 0
    for string in stringlist:
        total_chars = length_string
