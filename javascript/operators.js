let a = 10;
a += 1;
a % 2;
a++; //adds 1 to count

a--; //substract 1 to count

a === 10; // "equal"strict comparison

a !== 10; //  "not equal to" strict comparison

console.log(a === 10 && b === 21); //&& means "and"

console.log(a === 10 || b === 21); // || "or"

!true; // ! will return the oposite "return False"

console.log(a);